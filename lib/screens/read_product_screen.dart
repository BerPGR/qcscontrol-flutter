import 'package:flutter/material.dart';

class ReadProductScreens extends StatelessWidget {
  const ReadProductScreens({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
